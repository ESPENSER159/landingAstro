---
// Import the necessary dependencies
import { Image } from "astro:assets";
import PrimaryCTA from "@components/ui/buttons/PrimaryCTA.astro";
import SecondaryCTA from "@components/ui/buttons/SecondaryCTA.astro";
import ReviewComponent from "@components/ui/blocks/ReviewComponent.astro";
import Face from "../../lottie/Face";

// Define props from Astro
const {
  title,
  subTitle,
  primaryBtn,
  primaryBtnURL,
  secondaryBtn,
  secondaryBtnURL,
  withReview,
  avatars,
  starCount,
  rating,
  reviews,
  src,
  alt,
} = Astro.props;

// Define TypeScript interface for props
interface Props {
  title: string;
  subTitle?: string;
  primaryBtn?: string;
  primaryBtnURL?: string;
  secondaryBtn?: string;
  secondaryBtnURL?: string;
  withReview?: boolean;
  avatars?: Array<string>;
  starCount?: number;
  rating?: string;
  reviews?: string;
  src?: any;
  alt?: string;
}
---

<!-- Defining a grid container that holds all the content -->
<section
  id="home"
  class="mx-auto grid max-w-[85rem] gap-4 px-4 py-14 sm:px-6 md:grid-cols-2 md:items-center md:gap-8 lg:px-8 2xl:max-w-full"
>
  <!-- Title and description -->
  <div class="text-end">
    <video
      width="600"
      height="600"
      controls
      autoplay
      loop
      muted
      class="rounded-lg"
    >
      <source
        src="https://mtdevpocstorage.blob.core.windows.net/exposurehitss/simulacion_procesado.mp4?sp=r&st=2025-01-01T05:00:00Z&se=2025-12-31T05:00:00Z&sv=2022-11-02&sr=b&sig=MrMWHI9pAwotT0lDKlHp2YlM98EpY6YN1ATXr%2BfNg%2BY%3D"
        type="video/mp4"
      />
      Your browser does not support the video tag.
    </video>
  </div>
  <!-- Hero Image Section -->
  <div class="flex w-full justify-center">
    <div class="top-12 overflow-hidden">
      <div>
        <p class="text-pretty text-justify text-lg text-neutral-600 dark:text-neutral-400">
          La <span class="text-yellow-700 dark:text-yellow-400"
            >inteligencia artificial</span> ha transformado radicalmente la forma en que interactuamos con el mundo,
          y la visión por computadora es una de las áreas que más se ha beneficiado
          de estos avances. Gracias a algoritmos sofisticados y al poder de procesamiento
          de las computadoras, ahora es posible detectar y verificar objetos en imágenes
          y videos con una precisión sin precedentes. La <span class="text-yellow-700 dark:text-yellow-400"
          >detección de objetos</span> implica
          identificar y localizar objetos específicos dentro de una imagen o video.
          Esto se logra mediante redes neuronales convolucionales (CNN), que son
          capaces de extraer características visuales de las imágenes y clasificarlas.
        </p>
      </div>
    </div>
  </div>
</section>
