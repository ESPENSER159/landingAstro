---
// Define TypeScript interface for props
interface Props {
  title: string;
  subTitle?: string;
  benefits?: Array<string>;
}
---

<section
  id="product1"
  class="mx-auto max-w-[85rem] px-4 pb-10 pt-0 sm:px-6 lg:px-8 lg:pb-14 2xl:max-w-full"
>
  <div class="flex justify-evenly flex-col sm:flex-row">
    <div class="mx-2 flex justify-center text-center lg:col-span-7 mb-2 sm:mb-0">
      <video
        width="380"
        height="380"
        controls
        autoplay
        loop
        muted
        class="rounded-lg"
      >
        <source
          src="https://mtdevpocstorage.blob.core.windows.net/exposurehitss/manzana.mp4?sp=r&st=2024-09-17T21:25:22Z&se=2025-01-01T05:00:00Z&spr=https&sv=2022-11-02&sr=b&sig=hDVcPxARwunLvm6tH1KMeECBkLl5tfYITcl%2BbTPrT2Q%3D"
          type="video/mp4"
        />
        Your browser does not support the video tag.
      </video>
    </div>
    <div class="mx-2 flex justify-center text-center lg:col-span-7 mb-2 sm:mb-0">
      <video
        width="380"
        height="380"
        controls
        autoplay
        loop
        muted
        class="rounded-lg"
      >
        <source
          src="https://mtdevpocstorage.blob.core.windows.net/exposurehitss/banano.mp4?sp=r&st=2024-09-17T21:27:09Z&se=2025-01-01T05:00:00Z&spr=https&sv=2022-11-02&sr=b&sig=QRu7G%2FZTZx1ZP6d%2Bp1i5%2BC0WJc2fyoWyW1pkrUz30wI%3D"
          type="video/mp4"
        />
        Your browser does not support the video tag.
      </video>
    </div>
    <div class="mx-2 flex justify-center text-center lg:col-span-7">
      <video
        width="380"
        height="380"
        controls
        autoplay
        loop
        muted
        class="rounded-lg"
      >
        <source
          src="https://mtdevpocstorage.blob.core.windows.net/exposurehitss/naranja.mp4?sp=r&st=2024-09-17T21:25:52Z&se=2025-01-01T05:00:00Z&spr=https&sv=2022-11-02&sr=b&sig=%2FVdDVs%2FnBU202BxhQFJfuDqcKwQ8Hpb5UEYLnFVQtDM%3D"
          type="video/mp4"
        />
        Your browser does not support the video tag.
      </video>
    </div>
  </div>

  <script>
    import { gsap } from "gsap";

    type AnimationSettings = {
      autoAlpha?: number;
      y?: number;
      x?: number;
      willChange?: string;
    };

    function setElementAnimationDefaults(
      id: string,
      settings: AnimationSettings,
    ) {
      gsap.set(id, settings);
    }

    setElementAnimationDefaults("#fadeText", {
      autoAlpha: 0,
      y: 50,
      willChange: "transform, opacity",
    });

    setElementAnimationDefaults("#fadeInUp", {
      autoAlpha: 0,
      y: 50,
      willChange: "transform, opacity",
    });

    setElementAnimationDefaults("#fadeInMoveRight", {
      autoAlpha: 0,
      x: 300,
      willChange: "transform, opacity",
    });

    setElementAnimationDefaults("#fadeInMoveLeft", {
      autoAlpha: 0,
      x: -300,
      willChange: "transform, opacity",
    });

    let timeline = gsap.timeline({ defaults: { overwrite: "auto" } });

    timeline.to("#fadeText", {
      duration: 1.5,
      autoAlpha: 1,
      y: 0,
      delay: 1,
      ease: "power2.out",
    });

    timeline.to(
      "#fadeInUp",
      { duration: 1.5, autoAlpha: 1, y: 0, ease: "power2.out" },
      "-=1.2",
    );

    timeline.to(
      "#fadeInMoveRight",
      { duration: 1.5, autoAlpha: 1, x: 0, ease: "power2.inOut" },
      "-=1.4",
    );

    timeline.to(
      "#fadeInMoveLeft",
      { duration: 1.5, autoAlpha: 1, x: 0, ease: "power2.inOut" },
      "-=1.4",
    );

    timeline.to("#overlay", { duration: 1, autoAlpha: 0, delay: 0.2 });
  </script>
</section>
