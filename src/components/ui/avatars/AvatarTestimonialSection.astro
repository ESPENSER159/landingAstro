---
const { src, alt, svg } = Astro.props;

interface Props {
  src: string;
  alt: string;
  svg: string;
}

import Icon from "@components/ui/icons/Icon.astro";
---
<div class="flex-shrink-0">
  <!-- <img
    class="size-8 rounded-full sm:h-[2.875rem] sm:w-[2.875rem]"
    src={src}
    alt={alt}
    loading="lazy"
  /> -->
  
  <Icon name={svg} />
</div>
